# 变更记录

这里记录公开版本的行为变化。源码发布不表示已经提供公证安装包或完成所有真实服务验收。

## [0.2.3] — 2026-09-19

### 新增与变更

- 拆分 OpenAI 响应解析表达式，修复 Xcode 16.4 的类型推断编译失败，保持原解析规则。
- 提供可切换的 English / 简体中文 README，以及对应语言的界面截图。
- 统一为一个中英文 macOS 菜单栏应用，共用历史、调度、Keychain 和五类模型连接：Codex、OpenAI、Claude、Gemini、OpenAI 兼容 API。
- 补充本地图片恢复：导出失败后可重新保存，预览失败后可在本地重建；恢复不重新调用模型，也不覆盖已有外部导出。
- 区分原始 SVG、派生标注 SVG、PNG 预览、模型成功和文件导出失败；无法安全标注时保留原始 SVG。
- 修正模式与间隔设置的保存一致性：保存失败时不提交新的内存设置、下一次时间或待确认状态。
- Codex 固定请求显式禁用 `browser_use`、`computer_use` 和 `image_generation`；execution profile 会记录该边界，并由 fake CLI 回归检查参数。
- 保留固定提示词和首次响应记录，继续以人工查看图片为主；没有自动评分、排名或变化告警。

### 已知限制

- 这是 macOS 本地 MVP，没有自动更新流程，也没有公证后的分发安装包。
- 真实 Codex 请求已有验收记录；其他 provider 主要由协议样例和本地 HTTP 检查覆盖，不能据此声称所有真实 API 已验收。
- 通知送达、登录启动和物理睡眠唤醒仍需要系统交互验证；不同账号能否调用预填 model 取决于服务方权限。
- 单项 SVG 绘图不能代表模型整体能力，也不构成跨 provider 的等价排名。

## 准备文档

- [验证记录](docs/VALIDATION.md)
- [发布流程](docs/RELEASING.md)
